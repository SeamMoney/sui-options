# Wick Markets — THE single submission (sui-options monorepo). One repo, one app, a SUITE of games. ~6h.
# deepwap/bloxwap-clone is RETIRED — its tick-chart feel is being harvested into Wick's Ride. Nothing wasted.

LANES (disjoint files = zero collisions):
 1. FRONTEND (frontend/src) = the LOCAL UI-owner session (cohesive, low-clutter, screenshot-ready). FLEET: do NOT edit frontend/src.
 2. CONTRACTS/BACKEND = fleet: move/ (build+test+deploy; do NOT rewrite working modules), keeper/, bots/, sdk/, api/. All package IDs -> ~/work/uploads/addresses.md.
 3. ASSETS = fleet (write to ~/work/uploads): ONE logo, ONE video, ONE description, the in-depth README (in repo).
BRAND: "Wick Markets" only. No bloxwap/deepsurge/deepwap user-facing (one stray ref in frontend/src/routes/WickPro.tsx -> UI owner scrubs).
GAME SUITE: Wick Pro (/pro)=options, breakeven line + green/red AREA PnL. Ride (/ , /degen)=degen, LASERPOINTER-line PnL. Both on the fast tick chart, spot+predict, quick bets.
SUBMISSION: one link, one repo (-> transfers to maxmoneycash at the end), one logo, one video, one description.
