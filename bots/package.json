{
  "name": "wick-bots",
  "version": "0.1.0",
  "description": "Personality-driven testnet trading bots that create organic activity on Wick markets.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "setup": "tsx src/setup-bots.ts",
    "fund": "tsx src/setup-bots.ts fund-only",
    "balances": "tsx src/setup-bots.ts balances",
    "run": "tsx src/index.ts",
    "tick": "tsx src/index.ts tick"
  },
  "dependencies": {
    "@mysten/sui": "^2.16.0",
    "@wick/sdk": "file:../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
