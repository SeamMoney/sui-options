import { memo } from "react";

function MarketBiasPage() {
  return (
    <div className="flex h-full items-center justify-center">
      <p className="text-[11px] text-white/20">Market Bias</p>
    </div>
  );
}

export default memo(MarketBiasPage);
