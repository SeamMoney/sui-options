import { memo } from "react";

function ModelTrainerPage() {
  return (
    <div className="flex h-full items-center justify-center">
      <p className="text-[11px] text-white/20">Model Trainer</p>
    </div>
  );
}

export default memo(ModelTrainerPage);
